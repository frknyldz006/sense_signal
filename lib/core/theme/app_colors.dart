import 'package:flutter/material.dart';

class AppColors {
  /// Dark black / space grey background
  static const Color background = Color(0xFF0B0C10);

  /// Neon green accent
  static const Color neonGreen = Color(0xFF39FF14);

  /// Cyan accent
  static const Color cyan = Color(0xFF00FFFF);

  /// Red accent for alerts/errors
  static const Color neonRed = Color(0xFFFF003C);

  /// Subtle grid/ring color
  static const Color gridLine = Color(0x3339FF14);
}
